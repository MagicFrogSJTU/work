# 自动驾驶工作复盘分享：PPT 页面脚本

说明：本文件按“个人简介 -> 三个心得 -> 展望未来”的结构组织。每页只保留三部分：页标题、页内文字、图表。图表只使用两种形式：Markdown 图片引用，或 Mermaid 代码块。

---

## 第1页 自动驾驶算法发展切面：我的参与与思考

页内文字

- 姓名：陈奕志
- 籍贯：广东
- 学校：上海交大
- 工作年限：7年+
- 上家公司：华为车BU

---

## 第2页 我过去 5 年主要做了什么

页内文字：

- 华为前期：静态环境感知算法与数据闭环建设
- 华为后期：智驾大模型数据系统与团队建设
- 核心能力：算法研发、数据系统

图表：

```mermaid
flowchart LR
    B --> C[智驾大模型数据系统]
```

---

## 第3页 五年，三个心得

图表：

```mermaid
flowchart TB
    A[心得1<br/>the bitter lesson: 
算力+数据+简单通用学习算法，<br/>远胜过人类手工嵌入的领域知识、精巧特征工程与先验规则]
    A --> A1[神经网络吃掉传统软件]
    A --> A2[越端到端，系统能力上限越高]
    A --> A3[简单留给网络，复杂留给数据]
```

```mermaid
flowchart TB
    B[心得2<br/>当我们说“数据”时，是在说什么]
    B --> B1[数据定义，是算法问题]
    B --> B2[数据闭环，是工程问题]
    B --> B3[训练数据没有量，就没有真正的价值]
```

```mermaid
flowchart TB
    C[心得3<br/>AI应用是扬长避短的系统性工程]
    C --> C1[AI应用是扬长避短的系统性工程]
    C --> C2[预训练是大模型的知识瓶颈]
    C --> C3[上下文窗口是大模型的应用瓶颈]
```

---

## 第4页 心得1：the bitter lesson

页内文字：

- 算力 + 数据 + 简单通用学习算法
- 正在持续吃掉人工规则、特征工程和模块接口

图表：

```mermaid
flowchart TB
    A[the bitter lesson]
    A --> A1[神经网络吃掉传统软件]
    A --> A2[越端到端，系统能力上限越高]
    A --> A3[简单留给网络，复杂留给数据]
```

---

## 第5页 心得1-1：神经网络吃掉传统软件

页内文字：

- 2D: 算法各自为战，百万行后处理融合代码才是核心
- 3D: BEV / Occupancy大一统，后处理代码下降1个数量级

图表：

![2D 感知架构](./2d_ad_perception.png)

![3D 感知架构](./3d_ad_perception.png)

---

## 第6页 心得1-2：越端到端，系统能力上限越高

页内文字：

- 链路越长，接口损失和误差传递越重
- 端到端，把任务重写成统一学习问题

图表：

```mermaid
flowchart LR
    A[传感器 -> 感知 -> 预测 -> 规划 -> 控制] --> B[传感器 -> 端到端模型 -> 控制]
```

---

## 第7页 心得1-3：简单留给网络，复杂留给数据

页内文字：

- 模型架构更统一、简洁（文本/语音/视觉，分类/回归/生成）
- 但数据规模、团队规模、研发复杂度持续上升
- 以静态感知的数据处理为例：

图表：

```mermaid
flowchart LR
    subgraph A[在线推理]
        A1[模型] --> A2(道路语义推理结果)
    end
```

```mermaid
flowchart LR
    subgraph B[离线数据重建]
        B1(数据按地理聚类) --> B4
        B2[单趟重建<br/>推理 / 位姿优化 / 地图优化]
        B2 --> B3(单趟地图)
        B3 --> B4[多趟语义标量重建]
        B4 --> B5(3D标量重建地图)
        B5 -- 道路矢量标注 --> B6(道路语义地图)
        B6 -- 单趟重定位 --> B8(单趟语义GT)
    end
```

---

## 第8页 心得2：当我们说“数据”时，是在说什么

图表：

```mermaid
flowchart TB
    B[数据]
    B --> B1[数据定义，是算法问题]
    B --> B2[数据闭环，是工程问题]
    B --> B3[数据没有规模，就没有真正价值]
```

---

## 第9页 心得2-1：数据定义，是算法问题

页内文字：

- 输入输出，定义了"你要达到什么目的"，即任务本身。
- 评测指标，定义了”你认为什么是好的”，即任务的优化目标。
- 抛开这三个谈算法，是无源之水，镜中之月。

图表：

```mermaid
flowchart LR
    A[输入] --> D[任务定义]
    B[输出] --> D
    C[评测指标] --> D
    D --> E[好的设计是成功的一半]
```

---

## 第10页 心得2-2：数据闭环，是工程问题

页内文字：

- 数据理解：标注、统计、可视化、结构化
- 数据策划：挖掘、采集、合成、平衡
- 人工标注：
  - 质量重于数量. scaleAI/surgeAI标注员时薪可至200美元.
  - 成本高昂
    - 基于不信任的团队管理：规格/产线管理/工艺/质量
    - 基础设施：标注平台，人机共标算法，统计/运维后台

图表：

```mermaid
flowchart LR
    subgraph H[数据策划]
        C(存量-原始数据池) -- 挖掘 --> A[数据理解]
        G(流式数据管道) -- 采集 --> A[数据理解]
        E[数据合成] --> F
        F(结构化数据池) --> B[数据平衡]
        A[数据理解] --> F
    end
    B --> D[模型训练]
    D --> H
```

---

## 第11页 心得2-3：训练数据没有量，就没有真正的价值

页内文字：

- 训练数据价值，取决于存储、版本、检索、计算
- 大规模数据资产，决定模型迭代速度
- 我做过版本系统、检索系统、多模态数据仓和自动化产线

图表：

```mermaid
flowchart TB
    A[存储] --> E[训练数据价值]
    B[版本] --> E
    C[检索] --> E
    D[计算] --> E
```

---

## 第12页 心得3：AI应用是系统性工程

页内文字：

- AI 应用的核心不是单点最强，而是系统协同最优
- 大模型时代，这个问题进一步体现为知识瓶颈和上下文瓶颈

图表：

```mermaid
flowchart TB
    C[AI应用是系统性工程]
    C --> C1[取长补短]
    C --> C2[预训练是大模型的知识瓶颈]
    C --> C3[上下文窗口是大模型的应用瓶颈]
```

---

## 第13页 心得3-1：取长补短, 系统协同

页内文字：

- 超人模块 + 弱智AI，也能做出可用系统
- 激光雷达和 RoadCode（华为众包高精地图），都是典型例子
- 系统的竞争力，本质来自协同、互助。（算法、组织、生物体都一样）

图表：

![自动驾驶系统架构](./autonomous_driving_arch.png)

---

## 第14页 心得3-2：预训练是大模型的知识瓶颈

页内文字：

- 冷门垂域的微调有用，但瓶颈仍然在预训练
- 业界解垂域手段：大规模数据合成（冷启动：微调/蒸馏/挖掘/...）进预训练，挑选复杂/困难的数据进SFT和RLHF的循环迭代。

图表：

```mermaid
flowchart TB
    A[预训练]
    B[SFT]
    C[RLHF]
    A --> D[知识上限]
    B --> E[指令遵从]
    C --> F[偏好对齐]
    D --> G[垂域能力基础]
    E --> H[复杂任务基础</br>价值观对齐]
    F --> I[最终效果]
```

```mermaid
flowchart LR
    subgraph A[级联训练]
        A1[通用模型训练]
        A2[智驾垂域训练]
        A1 --> A2
    end
    subgraph B[联合训练]
        B1[盘古数据]
        B2[智驾数据]
        B3[联训：预训练+后训练]
        B1 --> B3
        B2 --> B3
    end
    A --> B
```

---

## 第15页 心得3-3：上下文窗口是大模型的应用瓶颈

页内文字：

- 大模型没有记忆，只有上下文
- 上下文工程的本质在减少单个 agent 的上下文负担

图表：

```mermaid
flowchart TB
    A[希望更长的上下文窗口] --> C[装下更多信息]
    B[希望更短的上下文窗口] --> D[保持任务性能]
    C --> E[应用矛盾]
    D --> E
    E --> F[skill / sub-agent / multi-agent / summary]
```

---

## 第16页 总结

文字：

- 数据是核心竞争力
- 垂域壁垒会一直存在

图表：

```mermaid
flowchart LR
    A[模型会更强] --> D[AI普惠化]
    B[应用会更简单] --> D
    C[数据系统会更难] --> E[数据是关键竞争力]
    D --> E
```

```mermaid
flowchart TB
    C["品牌信任"] --> F["垂域长期壁垒"]
    D["场景积累（行业知识库，真实数据，兜底经验，规模效应）"] --> F
    E["物理世界联接"] --> F
    F --> G["传统行业 AI 机会"]
```

---

## 第17页 展望未来

- 中远海运 x AI，机会巨大，值得期待！
- 预祝研究院做大做强！
- ANY QUESTIONS?
